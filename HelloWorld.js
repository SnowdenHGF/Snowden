const startYear = 2020;
const currentYear = new Date().getFullYear();
const devTime = currentYear - startYear;

console.log(
  `Hello World after ${result} years!` +
  `Let's create an unforgettable hello world!`
)
